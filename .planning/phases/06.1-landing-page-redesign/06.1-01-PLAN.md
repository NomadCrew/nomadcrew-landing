---
phase: 06.1-landing-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/styles/design-tokens.css
  - src/styles/global.css
  - src/assets/images/hero/layer-1-foreground.jpg
  - src/assets/images/hero/layer-2-midground.jpg
  - src/assets/images/hero/layer-3-background.jpg
autonomous: true

must_haves:
  truths:
    - "Lenis smooth scroll library is installed and available for import"
    - "Design tokens define warm minimalist color palette and typography scale"
    - "Three parallax layer images exist in hero folder"
  artifacts:
    - path: "src/styles/design-tokens.css"
      provides: "Warm minimalist CSS custom properties"
      contains: "--color-accent-primary"
    - path: "src/assets/images/hero/layer-1-foreground.jpg"
      provides: "Parallax foreground layer image"
    - path: "src/assets/images/hero/layer-2-midground.jpg"
      provides: "Parallax midground layer image"
    - path: "src/assets/images/hero/layer-3-background.jpg"
      provides: "Parallax background layer image"
  key_links:
    - from: "src/styles/global.css"
      to: "src/styles/design-tokens.css"
      via: "@import"
      pattern: "@import.*design-tokens"
---

<objective>
Install Lenis smooth scroll library, create warm minimalist design tokens, and source/prepare parallax hero images.

Purpose: Establish foundation for parallax hero section and consistent visual identity across all redesigned components.

Output: Dependencies installed, design system tokens defined, hero layer images ready for parallax implementation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-landing-page-redesign/06.1-CONTEXT.md
@.planning/phases/06.1-landing-page-redesign/06.1-RESEARCH.md
@src/styles/global.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Lenis and configure design tokens</name>
  <files>
    package.json
    pnpm-lock.yaml
    src/styles/design-tokens.css
    src/styles/global.css
  </files>
  <action>
1. Install Lenis smooth scroll library:
   ```bash
   pnpm add lenis
   ```

2. Create `src/styles/design-tokens.css` with warm minimalist design system:
   - Background colors: #FDFCFA (warm off-white), #F5F3EF (light beige), #EBE8E1 (taupe tint)
   - Accent color: #E87B4E (warm coral/terracotta), #D4653E (hover state)
   - Text colors: #2D2520 (warm near-black), #635750 (warm gray), #9B8F85 (lighter gray)
   - Typography: Inter font family with -apple-system fallback
   - Font weights: 400 (normal), 600 (semibold), 700 (bold)
   - Type scale using major third (1.250 ratio): xs through 4xl
   - Spacing scale based on 8px unit (0.5rem through 6rem)

3. Update `src/styles/global.css` to import design-tokens.css:
   - Add `@import "./design-tokens.css";` at the top
   - Keep existing Tailwind imports and @source directives

4. Verify installation with `pnpm run check` - should pass without TypeScript errors.
  </action>
  <verify>
    - `pnpm list lenis` shows lenis installed
    - `src/styles/design-tokens.css` exists with CSS custom properties
    - `pnpm run check` passes without errors
  </verify>
  <done>
    - Lenis library available for import in React components
    - Design tokens CSS file contains all color, typography, and spacing variables
    - global.css imports design-tokens.css
  </done>
</task>

<task type="auto">
  <name>Task 2: Source and prepare parallax hero images</name>
  <files>
    src/assets/images/hero/layer-1-foreground.jpg
    src/assets/images/hero/layer-2-midground.jpg
    src/assets/images/hero/layer-3-background.jpg
  </files>
  <action>
1. Create the hero images directory: `src/assets/images/hero/`

2. Source 3 high-quality travel photography images from Unsplash that fit the "friends in beautiful places" theme:
   - **Background layer (layer-3):** Wide landscape/destination shot (mountains, beach, city skyline)
   - **Midground layer (layer-2):** Medium shot with environmental context (trails, streets, scenic overlooks)
   - **Foreground layer (layer-1):** Group of travelers/friends or travel elements (silhouettes, backpacks, people from behind looking at view)

3. Download using Unsplash Source API or direct links:
   - Use landscape orientation (recommended: 1920x1080 or wider)
   - Quality: high (Unsplash provides optimized downloads)
   - Search terms: "friends travel mountain", "group adventure landscape", "travelers scenic view"

4. Recommended Unsplash images (verify these are available, replace if not):
   - Background: https://unsplash.com/photos/a-group-of-people-standing-on-top-of-a-mountain-x8ZStukS2PM (mountain landscape)
   - Midground: https://unsplash.com/photos/person-standing-on-cliff-g0Cvtrt5OJ4 (cliff/scenic)
   - Foreground: https://unsplash.com/photos/two-person-standing-on-gray-tile-paving-BiQvtuv6iPc (travelers looking out)

   Alternative approach if specific images unavailable:
   - Search "travel friends" on unsplash.com, download 3 complementary landscape images
   - Ensure images have different depth/focal points for parallax effect

5. Save images with compression appropriate for web (aim for <500KB each):
   - Use Sharp or online tools if needed to resize/compress
   - Target dimensions: 1920x1080 minimum width for quality on large displays

6. Verify images are properly saved:
   ```bash
   ls -la src/assets/images/hero/
   ```
  </action>
  <verify>
    - `ls src/assets/images/hero/` shows all 3 layer images
    - Each image file exists and is a valid JPEG
    - Combined file size reasonable for web (<1.5MB total)
  </verify>
  <done>
    - Three parallax layer images exist in src/assets/images/hero/
    - Images represent "friends in beautiful places" theme
    - Images are web-optimized (reasonable file sizes)
  </done>
</task>

</tasks>

<verification>
- [ ] `pnpm list lenis` confirms library installed
- [ ] `src/styles/design-tokens.css` contains CSS custom properties for colors, typography, spacing
- [ ] `src/styles/global.css` imports design-tokens.css
- [ ] `src/assets/images/hero/` contains 3 JPEG images
- [ ] `pnpm run check` passes
- [ ] `pnpm run dev` starts without errors
</verification>

<success_criteria>
1. Lenis smooth scroll library is installed and can be imported
2. Design tokens file defines complete warm minimalist palette (5+ colors, 7+ type sizes, 6+ spacing values)
3. Three hero layer images exist and are optimized for web
4. All existing functionality preserved (dev server starts, check passes)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-landing-page-redesign/06.1-01-SUMMARY.md`
</output>
