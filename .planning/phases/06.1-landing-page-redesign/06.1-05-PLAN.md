---
phase: 06.1-landing-page-redesign
plan: 05
type: execute
wave: 3
depends_on: ["06.1-02", "06.1-03", "06.1-04"]
files_modified:
  - src/pages/index.astro
  - src/components/react/Navbar.tsx
autonomous: false

must_haves:
  truths:
    - "Landing page displays parallax hero with travel photography"
    - "Features section uses bento grid layout"
    - "Social proof section is visible above waitlist form"
    - "Footer is expanded with navigation and social links"
    - "Typography and colors follow warm minimalist design system"
    - "Mobile responsive design maintains visual impact"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete redesigned landing page"
      min_lines: 50
  key_links:
    - from: "src/pages/index.astro"
      to: "ParallaxHero, BentoFeatures, SocialProof, ExpandedFooter"
      via: "React island imports"
      pattern: "ParallaxHero|BentoFeatures|SocialProof|ExpandedFooter"
    - from: "src/pages/index.astro"
      to: "SmoothScrollProvider"
      via: "wrapper component"
      pattern: "SmoothScrollProvider"
---

<objective>
Integrate all redesigned components into the landing page, replacing the primitive layout with the conversion-optimized experience.

Purpose: Complete the landing page redesign by assembling ParallaxHero, BentoFeatures, SocialProof, ExpandedFooter into index.astro.

Output: Fully redesigned landing page with all UI requirements satisfied and visual verification checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-landing-page-redesign/06.1-CONTEXT.md
@.planning/phases/06.1-landing-page-redesign/06.1-RESEARCH.md
@.planning/phases/06.1-landing-page-redesign/06.1-02-SUMMARY.md
@.planning/phases/06.1-landing-page-redesign/06.1-03-SUMMARY.md
@.planning/phases/06.1-landing-page-redesign/06.1-04-SUMMARY.md
@src/pages/index.astro
@src/components/react/Navbar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Navbar for warm minimalist styling</name>
  <files>
    src/components/react/Navbar.tsx
  </files>
  <action>
Update `src/components/react/Navbar.tsx` to align with redesign:

1. Review current Navbar styling and update colors to match design tokens:
   - Background: transparent initially, warm off-white on scroll (if not already)
   - Text: warm near-black (#2D2520) instead of gray-900
   - CTA button: Use accent color (#E87B4E / orange-500)
   - Hover states: Use accent color for links

2. Ensure Navbar works with the taller parallax hero:
   - Navbar should remain fixed at top
   - Transparent background on parallax section, solid when scrolled
   - If Navbar already handles scroll-based background change, verify it works

3. Minimal changes - only update colors/styling to match warm minimalist palette:
   - Replace any gray-900 with warm text color
   - Ensure CTA "Join Waitlist" uses orange-500 or design token
   - Verify mobile menu styling matches new palette

4. Add z-50 or higher z-index if not present (to stay above parallax layers)

5. Keep all existing functionality (mobile menu, scroll behavior, animations)
  </action>
  <verify>
    - `pnpm run check` passes
    - Navbar colors updated to warm palette
    - Navbar has sufficient z-index (z-50+)
  </verify>
  <done>
    - Navbar styling matches warm minimalist design system
    - Navbar works correctly above parallax hero
    - All existing functionality preserved
  </done>
</task>

<task type="auto">
  <name>Task 2: Assemble redesigned landing page</name>
  <files>
    src/pages/index.astro
  </files>
  <action>
Replace `src/pages/index.astro` content with redesigned layout:

1. Update imports:
   ```astro
   ---
   import BaseLayout from '../layouts/BaseLayout.astro';
   import Navbar from '../components/react/Navbar.tsx';
   import SmoothScrollProvider from '../components/react/SmoothScrollProvider.tsx';
   import ParallaxHero from '../components/react/ParallaxHero.tsx';
   import BentoFeatures from '../components/react/BentoFeatures.tsx';
   import SocialProof from '../components/react/SocialProof.tsx';
   import WaitlistForm from '../components/react/WaitlistForm.tsx';
   import ExpandedFooter from '../components/react/ExpandedFooter.tsx';
   ---
   ```

2. Page structure with hydration strategies:

   a. **BaseLayout:**
      - Keep existing SEO meta (title, description, isHomepage)
      - Title: "Group Travel, Simplified" (or updated tagline)

   b. **Navbar:** `client:load` (immediate interactivity)

   c. **SmoothScrollProvider:** `client:load` (wraps content for smooth scroll)
      - Wraps: ParallaxHero, and potentially extends to whole page

   d. **ParallaxHero:** `client:load` (needs immediate scroll tracking)
      - Contains hero imagery and bottom-reveal text/CTA
      - Note: CTA in hero links to #waitlist

   e. **BentoFeatures:** `client:visible` (below hero, deferred)
      - id="features" for navigation

   f. **SocialProof:** `client:visible` (below features)
      - Positioned above waitlist form to reinforce conversion

   g. **Waitlist Section:** `client:visible`
      - id="waitlist" for CTA links
      - Keep WaitlistForm component
      - Update surrounding section styling to match warm palette

   h. **ExpandedFooter:** `client:idle` (low priority hydration)

3. Example assembled structure:
   ```astro
   <BaseLayout
     title="Group Travel, Simplified"
     description="Coordinate plans, share locations, manage expenses, and chat in real-time - all within a single, mobile-first app designed for modern group travel."
     isHomepage={true}
   >
     <Navbar client:load />

     <SmoothScrollProvider client:load>
       <ParallaxHero client:load />
     </SmoothScrollProvider>

     <BentoFeatures client:visible />

     <SocialProof client:visible />

     <section id="waitlist" class="py-16 px-4 bg-[var(--color-bg-primary)]">
       <div class="container mx-auto max-w-xl text-center">
         <h2 class="text-3xl font-bold mb-6 text-[var(--color-text-primary)]">
           Join the Waitlist
         </h2>
         <p class="text-[var(--color-text-secondary)] mb-8">
           Be among the first to experience a better way to travel together.
           Get early access and exclusive updates.
         </p>
         <WaitlistForm client:visible />
       </div>
     </section>

     <ExpandedFooter client:idle />
   </BaseLayout>
   ```

4. Style updates for waitlist section:
   - Use design token CSS variables for colors
   - Or use Tailwind equivalents that match the warm palette

5. Remove old components that are being replaced:
   - Remove old HeroSection import
   - Remove old FeatureCard usage
   - Remove old Footer.astro import

6. Verify IDs for navigation:
   - #features on BentoFeatures section
   - #waitlist on waitlist section
   - These should work with Navbar links and hero CTA
  </action>
  <verify>
    - `pnpm run check` passes
    - `pnpm run dev` starts without errors
    - http://localhost:4321/ shows redesigned landing page
    - All new components render (hero, bento, social proof, footer)
    - No hydration errors in browser console
    - Navigation links (#features, #waitlist) scroll to correct sections
  </verify>
  <done>
    - Landing page displays parallax hero with travel imagery
    - Bento grid features section below hero
    - Social proof section visible
    - Waitlist form section intact
    - Expanded footer at bottom
    - All sections use warm minimalist styling
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete landing page redesign with:
- Multi-layer parallax hero with travel photography
- Bottom-reveal text and CTA
- Bento grid feature showcase
- Social proof section
- Expanded footer with navigation and social links
- Warm minimalist design system (colors, typography, spacing)
  </what-built>
  <how-to-verify>
1. Open http://localhost:4321/ in browser
2. Verify parallax hero:
   - [ ] Travel photography images visible
   - [ ] Scroll creates layered depth effect (images move at different speeds)
   - [ ] Text/CTA reveals at bottom of hero on scroll
   - [ ] "Join the Waitlist" CTA button is clickable
3. Verify bento features:
   - [ ] Grid has variable card sizes (large, wide, small)
   - [ ] All 4 features displayed with icons
   - [ ] Hover effects on cards
4. Verify social proof:
   - [ ] Section visible between features and waitlist
   - [ ] Shows trust messaging or count
5. Verify footer:
   - [ ] Multiple columns with links
   - [ ] Social media icons present
   - [ ] Copyright 2026 visible
6. Verify navigation:
   - [ ] Click Navbar "Features" scrolls to features section
   - [ ] Click "Join Waitlist" (hero or navbar) scrolls to waitlist
7. Verify responsiveness:
   - [ ] Resize to mobile width - layout adapts
   - [ ] Parallax reduced/disabled on mobile (optional)
   - [ ] Footer stacks to single column
8. Verify accessibility:
   - [ ] Enable "Reduce Motion" in OS settings
   - [ ] Refresh page - parallax should be disabled
9. Verify design system:
   - [ ] Warm color palette (off-white backgrounds, coral accent)
   - [ ] Consistent typography
   - [ ] Hover states on interactive elements
  </how-to-verify>
  <resume-signal>
Type "approved" if redesign meets expectations, or describe specific issues to fix.
  </resume-signal>
</task>

</tasks>

<verification>
- [ ] `src/pages/index.astro` updated with all new components
- [ ] `src/components/react/Navbar.tsx` updated for warm palette
- [ ] `pnpm run check` passes
- [ ] `pnpm run dev` starts without errors
- [ ] Landing page displays at http://localhost:4321/
- [ ] ParallaxHero visible with layered images
- [ ] BentoFeatures shows 4 features in grid
- [ ] SocialProof displays trust messaging
- [ ] ExpandedFooter has navigation columns and social icons
- [ ] Human verification checkpoint approved
</verification>

<success_criteria>
1. Hero section includes compelling visual with parallax and social proof (UI-01 partial)
2. Features displayed in bento grid layout with variable card sizes (UI-02)
3. At least one form of social proof visible (UI-03)
4. Footer includes product links, company links, and social media icons (UI-04)
5. Consistent typography system using warm palette (UI-05)
6. All interactive elements have appropriate hover/focus states
7. Mobile responsive design maintains visual impact
8. Human verification confirms redesign meets expectations
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-landing-page-redesign/06.1-05-SUMMARY.md`
</output>
